#!/bin/bash
publicdir=/app
appdir=/laravel
# if [ ! -d /tmp ] ; then
# mkdir /tmp
# fi
#
# echo $tmpdir
# #read isc
# echo $publicdir"<<workdir"
# #read isc
# if [ -d $tmpdir ] ; then
# rm -Rf $tmpdir
# fi
# if [ -d $publicdir ] ;then
# rm -Rf $publicdir/*
# mkdir -p $publicdir
# fi
#
# composer create-project --no-audit --prefer-dist laravel/laravel:^7.0 $appdir
#
# echo "tryng copy files."
# cp -Rf $tmpdir/* $publicdir/
# #rm -Rf $tmpdir
# chmod 777 $appdir -R
# composer2 install -d $appdir || composer2 update -d $appdir
# composer update -d $appdir 2>> /dev/null
# #cd $publicdir
# rm -rf /app
# ls -S -T $appdir/public $publicdir
